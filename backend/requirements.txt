# FastAPI Server Dependencies (Used in main.py)
fastapi
uvicorn[standard]  # [standard] includes dependencies like httptools and ujson for better performance
pydantic           # Used for the BaseModel imports (ChatRequest, ChatResponse, etc. in models.py)
python-multipart   # Used by FastAPI for form data, good to include
python-dotenv      # Used for load_dotenv()

# LangChain and RAG Dependencies (Used in rag.py)
langchain-core
langchain-community
langchain-text-splitters
langchain-chroma
langchain-mistralai

# CRITICAL for the create_..._chain imports in rag.py
langchain-classic

# Underlying ML/Vector Store/File Handling Libraries
sentence-transformers # For HuggingFaceEmbeddings
chromadb              # For Chroma vector store
pypdf                 # For PyPDFLoader
